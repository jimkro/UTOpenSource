#include <bits/stdc++.h>
using namespace std;

void solve(){
    int k, x; cin >> k >> x;
    cout << x * k + 1 << "\n";
}

int main(){
    ios::sync_with_stdio(0); cin.tie(0);
    int T; cin >> T;
    while(T--) solve();
}